# Copy this file to config.properties and adjust values locally.

# Auth mode can be: file, db, or hybrid
auth.mode=hybrid

# Database settings (used when auth.mode is db or hybrid)
db.url=jdbc:mysql://localhost:3306/electricity_billing
db.user=root
db.password=


